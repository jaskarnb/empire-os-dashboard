import React from 'react'
import ReactDOM from 'react-dom/client'
import EmpireOS from './EmpireOS.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode><EmpireOS /></React.StrictMode>
)
